const navToggle=document.querySelector('.nav-toggle');const navLinks=document.querySelector('.nav-links');const toast=document.querySelector('.toast');const bootOverlay=document.getElementById('bootOverlay');navToggle?.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>{navLinks?.classList.remove('open');navToggle?.setAttribute('aria-expanded','false');}));document.querySelectorAll('[data-placeholder-link]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();toast?.classList.add('show');clearTimeout(window.__mechosToast);window.__mechosToast=setTimeout(()=>toast?.classList.remove('show'),2600);}));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const targets=document.querySelectorAll('[data-reveal]');if('IntersectionObserver'in window){const ob=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');ob.unobserve(entry.target);}}),{threshold:.12});targets.forEach(el=>ob.observe(el));}else targets.forEach(el=>el.classList.add('is-visible'));document.querySelectorAll('.tilt-card').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();const y=((e.clientX-r.left)/r.width-.5)*7;const x=((e.clientY-r.top)/r.height-.5)*-7;card.style.transform=`perspective(1200px) rotateX(${x}deg) rotateY(${y}deg) translateY(-3px)`;});card.addEventListener('mouseleave',()=>card.style.transform='');});window.addEventListener('load',()=>{if(sessionStorage.getItem('mechosBooted')){bootOverlay?.classList.add('hidden');return;}setTimeout(()=>{bootOverlay?.classList.add('hidden');sessionStorage.setItem('mechosBooted','1');},2800);});async function hydratePackedImages(){const nodes=[...document.querySelectorAll('[data-b64-src]')];await Promise.all(nodes.map(async node=>{try{const res=await fetch(node.dataset.b64Src);if(!res.ok)return;const b64=(await res.text()).trim();node.src=`data:image/jpeg;base64,${b64}`;}catch(_){}}));}hydratePackedImages();const heroVideo=document.querySelector('.hero-video');const heroFallback=document.querySelector('.hero-fallback');window.addEventListener('scroll',()=>{const y=window.scrollY;if(heroVideo)heroVideo.style.transform=`translateY(${y*.08}px) scale(1.06)`;if(heroFallback)heroFallback.style.transform=`translateY(${y*.05}px) scale(1.04)`;},{passive:true});